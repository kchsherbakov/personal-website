import styled from "styled-components";

const ListItemLinks = styled.div`
    display: flex;
    flex-direction: row;
    gap: 1rem;
    
    & img {
        width: 12px;
        margin-right: 0.2rem;
    }
`;

export default ListItemLinks;
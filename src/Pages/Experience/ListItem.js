import styled from "styled-components";

const ListItem = styled.div`
    display: flex;
    flex-direction: row;
    gap: 1rem;
    margin-left: -6rem;
    margin-right: -6rem;
`;

export default ListItem;
